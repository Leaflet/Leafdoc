{{#if name}}
"{{name}}" [ label = <<TABLE CELLSPACING="0">
	<TR><TD><B>{{name}}</B></TD></TR>
	{{{supersections}}}
</TABLE>>]

{{#each inherits}}
	"{{../name}}" -> "{{this}}"
{{/each}}

{{else}}
{{! Miniclasses don't have a name, skip them }}
{{/if}}
